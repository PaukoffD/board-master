<p id="notice"><%= notice %></p>

<h1>Доска объявлений</h1>

<br>

<%= link_to 'New Notice', new_notice_path, :class => 'btn btn-primary' %>
<table class="table table-striped table-hover ">

<h4>Последние объявления</h4>
 <thead>
    <tr>
	<th width="10%">Фото</th>
	<th width="40%">Объявление</th>
    <th width="10%">Фото</th>
	<th width="40%"><center>Объявление</center></th>
     </tr>
  </thead>
  
    <tbody>
  <% for notice in @notices %>
    <tr>
      <td>
	 <div class='icon'><%= image_tag(notice.avatar.url(:thumb), class: 'avatar', :size => 30) %></div>
	 </td>
	  
	  
	  <td><h5><%= link_to h(notice.notice), notice_path(notice.id) %> </td>
	
	  <td>
	
	 
	 <div class='icon'><%= image_tag(notice.avatar.url(:thumb), class: 'avatar', :size => 30) %></div>
	 </td>
	  
	  
	  <td><h5><%= link_to h(notice.notice), notice_path(notice.id) %> </td>
	  </br> 
	  
	  
	  
      
    </tr>
  <% end %>
   </tbody>
</table>


 


